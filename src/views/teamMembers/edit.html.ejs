<div id="body-container">
  <div class="jumbotron-center">
    <div class="box-container invitation-container">
      <h1> Editar jugador del equipo %></h1>

      <%- include('../_error/snippet', { errors: locals.errors }) %>

      <div>
        <%= teamMember.name %>
        <form action="<%= submitTeamMemberPath %>" method="post">
          <div>
            <input type="hidden" name="_method" value="patch" />
          </div>
          <div>
            <label for="isCaptain"></label>
            <input type="checkbox" name="isCaptain" <%= (teamMember.isMember.isCaptain) ? 'checked' : '' %> /> Capitán
          </div>
          <div>
            <input type="submit" name="update" value="Guardar Cambios" />
          </div>
        </form>
      </div>

      <%- include('../_buttons/delete', { deletePath: deleteTeamMemberPath }) %>

      <%- include('../_buttons/cancel', { cancelPath }) %>

    </div>
  </div>
</div>
