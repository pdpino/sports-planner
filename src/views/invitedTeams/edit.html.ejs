<div class ="center">
  <h1> Editar invitación a equipo %></h1>

  <%- include('../error-snippet', { errors: locals.errors }) %>

  <div>
    <%= team.name %>

    <%- include('../_invitations/form', {
      invitation: team.isTeamInvited,
      chooseStatuses,
      submitPath: submitInvitedTeamPath,
      hasAdminPermission: false, // REVIEW: the invitedTeams do not require this
    }) %>

  </div>

  <form action="<%= deleteInvitedTeamPath %>" method="post">
    <div>
      <input type="hidden" name="_method" value="delete" />
    </div>
    <div>
      <input class"deletebutton" type="submit" name="delete" value="Eliminar" />
    </div>
  </form>

  <form action=<%= cancelPath %>>
    <input type="submit" value="Cancelar"/>
  </form>

</div>
