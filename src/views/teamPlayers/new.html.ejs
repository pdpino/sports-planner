<h1>Agregar Jugadores</h1>

<% if(teamsNotPlayed.length > 0) { %>
  <form action="<%= submitTeamPlayerPath %>" method="post">
    <div>
      <label for="playerId">Elegir Jugadores</label>
      <% teamsNotPlayed.forEach( (player) => { %>
        <div>
          <input type="radio" name="playerId" value="<%= player.id %>"/> <%= player.name %>
        </div>
      <% }) %>
    </div>

    <div>
      <input type="submit" name="create" value="Añadir" />
    </div>

  </form>
<% } else { %>
  <div>
    <strong>
      No quedan jugadores para agregar!
    </strong>
  </div>
<% } %>

<form action="<%= cancelPath %>" method="get">
    <input type="submit" value="Cancelar"/>
</form>
