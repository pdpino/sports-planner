<%# Provide: errors%>

<% if (errors) { %>
  <div class="error error-list">
    Necesitas arreglar los siguiente errores:
    <ul>
      <% errors.forEach(error => { %>
        <li><%= error.message %></li>
      <% }); %>
    </ul>
  </div>
<% } %>
