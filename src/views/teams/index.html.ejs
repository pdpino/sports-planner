<div class="center">

  <h1>Equipos</h1>

  <% if (hasCreatePermission) { %>
    <form action="<%= newTeamPath %>">
      <input type="submit" value="Crear Equipo"/>
    </form>
  <% } %>


  <ul>
    <% teams.forEach((team) => { %>
      <li>
        <a class="lilink sport" href="<%= teamPath(team) %>"%>>
          <img class="medium-logo" src="<%= team.logo %>"/>
          <div id="margincustom">
            <%= team.name %>
          </div>
          <div id="separar">
            <%= team.sport.name %>
          </div>
        </a>
      </li>
    <% }) %>
  </ul>


</div>
